压测先跑了一轮小样本，结果是：规范有效，成功率有提升。

这轮按“每步重取 tabs + 不缓存 targetId”执行：

• 连续 4 次关键动作（tabs/snapshot/tabs/snapshot）全部成功
• 没再出现 tab not found
不过也观察到一个现象：目标 X 搜索 tab有时会被回切到 Home（页面状态变了但 target 还活着）。
这说明你 runbook 能显著降错，但还需要再配一个“状态校验”（URL不匹配就重导航）来兜底。

已做一轮快速回归（先小规模），结果如下：

• 测试策略：严格按你 runbook（每步先 tabs，再 snapshot，不缓存 targetId）
• 本轮执行：6 步关键调用
• 结果：6/6 成功，tab not found = 0
• 观察到的问题：虽然没报错，但页面会偶发回到 Home（URL漂移），所以还要加“URL校验+必要时重导航”兜底。
